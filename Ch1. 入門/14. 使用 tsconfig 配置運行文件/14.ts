/* 運行 tsc 有兩種方法*/
/* 第一種 terminal => run task => 選擇對應資料夾 (typescript) => tsc: watch 要監聽的配置項*/
/* 第二種 在當前 folder 使用 terminal 輸入 tsc -w。就會監聽所有 .ts 後綴的文件 */

let hd: string = 'houdunren';

console.log(hd);
